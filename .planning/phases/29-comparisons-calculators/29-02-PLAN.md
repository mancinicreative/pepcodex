---
phase: 29-comparisons-calculators
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - src/content/comparisons/*.mdx (180 new files + 80 updated)
autonomous: true

must_haves:
  truths:
    - "180 new comparison MDX files exist in src/content/comparisons/"
    - "All 280 comparison files have faqs field in frontmatter"
    - "No duplicate comparison pairs exist (checking both directions)"
    - "All peptideA and peptideB slugs in new comparisons match existing peptide dossier slugs"
    - "Each new comparison has at minimum: Overview, Evidence Comparison, Mechanism Comparison, Regulatory Status, Key Differences, Summary sections"
  artifacts:
    - path: "src/content/comparisons/"
      provides: "280 total comparison MDX files (100 existing + 180 new)"
      min_lines: 280
    - path: "src/content/comparisons/*.mdx"
      provides: "Each file has faqs array with 4 FAQ items"
      contains: "faqs:"
  key_links:
    - from: "src/content/comparisons/*.mdx"
      to: "src/content/peptides/*.mdx"
      via: "peptideA and peptideB slugs must match peptide file slugs"
      pattern: "peptideA|peptideB"
    - from: "src/content/comparisons/*.mdx"
      to: "src/layouts/ComparisonLayout.astro"
      via: "rendered by ComparisonLayout through [...slug].astro"
      pattern: "category.*lastUpdated"
---

<objective>
Generate 180 new comparison MDX files and backfill FAQs to the 80 existing comparisons that are missing them.

Purpose: This is the largest content generation task in Phase 29, adding 180 new comparison pages to reach 280 total. Every comparison needs FAQs for Google FAQ rich results (schema markup already wired in ComparisonLayout from Phase 24). This brings the site from ~878 to ~1,058 pages.

Output:
- 180 new comparison MDX files in src/content/comparisons/
- 80 existing comparison files updated with FAQ frontmatter
- Total: 280 comparisons, all with FAQs
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-comparisons-calculators/29-RESEARCH.md
@.planning/phases/29-comparisons-calculators/29-01-SUMMARY.md

Key references:
- scripts/generate-comparisons.mjs (created in Plan 01 -- run this)
- scripts/backfill-comparison-faqs.mjs (created in Plan 01 -- run this)
- src/content/comparisons/ (100 existing files, target 280)
- src/content/peptides/ (92 peptide dossiers, data source for comparisons)
- src/content/config.ts (comparison schema with optional faqs field)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run comparison generation script to create 180 new MDX files</name>
  <files>src/content/comparisons/*.mdx (180 new files)</files>
  <action>
Run the comparison generation script created in Plan 01:

```bash
cd peptide-library
node scripts/generate-comparisons.mjs
```

After the script completes:
1. Verify the count: `ls src/content/comparisons/*.mdx | wc -l` should be ~280
2. Spot-check 5 random new files to verify:
   - Frontmatter has all required fields (title, peptideA, peptideB, category, lastUpdated, summary, metaTitle, metaDescription, faqs)
   - faqs array has 4 entries with question/answer pairs
   - Body has all 7 sections (Overview, Evidence Comparison, Mechanism Comparison, Regulatory Status, Safety Concerns or equivalent, Key Differences, Summary)
   - peptideA and peptideB slugs match actual files in src/content/peptides/
   - File naming follows `{slug-a}-vs-{slug-b}.mdx` pattern
3. Check for any pairs where BOTH directions exist (e.g., `a-vs-b.mdx` AND `b-vs-a.mdx` among the NEW files). Some existing files intentionally have both directions -- that's fine. But NEW files should not create duplicates.

If the script produces fewer than 170 or more than 190 files, investigate. The target is 180 but the exact count depends on how many valid pairs the algorithm finds after exclusions.

If any issues are found, fix the script and re-run. Delete any malformed outputs before re-running.

MEMORY WARNING: With 280 MDX files, the Astro build may need more memory. If `npm run build` fails with "JavaScript heap out of memory", set `NODE_OPTIONS=--max-old-space-size=4096` before building.
  </action>
  <verify>
1. `ls src/content/comparisons/*.mdx | wc -l` returns ~280 (100 existing + ~180 new)
2. Spot-check 3 new files: each has valid YAML frontmatter with faqs, and markdown body with required sections
3. Run a quick validation: `node -e "const fs=require('fs');const files=fs.readdirSync('src/content/comparisons').filter(f=>f.endsWith('.mdx'));console.log('Total:',files.length)"` shows ~280
  </verify>
  <done>
~180 new comparison MDX files exist in src/content/comparisons/. Each has complete frontmatter (including 4 FAQs) and substantive body content with all required sections. Total comparison count is ~280.
  </done>
</task>

<task type="auto">
  <name>Task 2: Backfill FAQs to 80 existing comparisons and verify build</name>
  <files>src/content/comparisons/*.mdx (80 existing files updated)</files>
  <action>
Run the FAQ backfill script created in Plan 01:

```bash
cd peptide-library
node scripts/backfill-comparison-faqs.mjs
```

After the script completes:
1. Verify that ~80 files were updated (script should report this)
2. Spot-check 3 updated files:
   - Open a file that previously had NO faqs field
   - Confirm faqs array now exists with 4 entries
   - Confirm the rest of the frontmatter is unchanged (title, peptideA, peptideB, category, etc.)
   - Confirm the body content is unchanged

3. Verify ALL 280 comparisons now have FAQs:
```bash
# Count files WITH faqs
grep -l "^faqs:" src/content/comparisons/*.mdx | wc -l
# Should match total file count (~280)
```

4. Run the full build to verify everything works:
```bash
cd peptide-library
npm run build
```

If build fails with memory errors:
```bash
NODE_OPTIONS=--max-old-space-size=4096 npm run build
```

If build fails with Zod validation errors, inspect the failing file(s) and fix frontmatter issues. Common issues:
- Missing required fields (title, peptideA, peptideB, category, lastUpdated, summary)
- Invalid category value (must be one of: metabolic, repair-recovery, hormonal, longevity, cognitive, immune, other)
- Invalid date format for lastUpdated
- Malformed YAML in faqs array (indentation, quoting)

5. If the build requires the NODE_OPTIONS flag, update `package.json` scripts to include it:
```json
"build": "NODE_OPTIONS=--max-old-space-size=4096 astro build"
```
(Only if needed -- don't preemptively add it.)
  </action>
  <verify>
1. `grep -rl "^faqs:" src/content/comparisons/ | wc -l` equals total comparison file count (~280)
2. `npm run build` completes successfully (with or without NODE_OPTIONS)
3. No Zod validation errors in build output
4. Spot-check 2 backfilled files: FAQs present, original content intact
  </verify>
  <done>
All ~280 comparison files have FAQs. The 80 previously-missing FAQs are backfilled. Build completes successfully. No content was lost or corrupted during the backfill process.
  </done>
</task>

</tasks>

<verification>
1. Total comparison files: ~280 (100 existing + ~180 new)
2. All 280 files have `faqs:` in frontmatter
3. `npm run build` passes
4. New files have valid frontmatter matching the comparison schema in config.ts
5. No duplicate pairs among new files
6. All peptideA/peptideB slugs reference real peptide dossier files
</verification>

<success_criteria>
- 180 new comparison MDX files generated with FAQs and substantive content
- 80 existing comparisons backfilled with FAQs
- All 280 comparisons validate against the Zod schema
- Build completes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/29-comparisons-calculators/29-02-SUMMARY.md`
</output>
